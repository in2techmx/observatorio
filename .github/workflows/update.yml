- name: Push results
        run: |
          git config --global user.name 'NewsBot'
          git config --global user.email 'bot@antigravity.news'
          # Esta línea asegura que git registre el archivo aunque sea nuevo
          git add latest_news.json || touch latest_news.json && git add latest_news.json
          git commit -m "Update news data" || echo "No changes to commit"
          git push
